{
    "name": "subsets/algorithm-subsets",
    "description": "algorithm that generates and prints on the console all the subsets of size k",
    "minimum-stability": "stable",
    "license": "proprietary",
    "authors": [
        {
            "name": "Rayner Luis"
        }
    ],
    "require": {
        "php": "^7.4",
        "symfony/console": "^5.2",
        "psr/log": "^1.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "Subsets\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}
